<form method="post" action="{{ path('medical_treatment_delete', {'id': medical_treatment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medical_treatment.id) }}">
    <button class="btn">Delete</button>
</form>
