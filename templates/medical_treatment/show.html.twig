{% extends 'base.html.twig' %}

{% block title %}MedicalTreatment{% endblock %}

{% block body %}
    <h1>MedicalTreatment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medical_treatment.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ medical_treatment.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('medical_treatment_index') }}">back to list</a>

    <a href="{{ path('medical_treatment_edit', {'id': medical_treatment.id}) }}">edit</a>

    {{ include('medical_treatment/_delete_form.html.twig') }}
{% endblock %}
